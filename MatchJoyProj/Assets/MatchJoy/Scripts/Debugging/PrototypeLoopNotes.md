# Prototype Loop Notes

This folder is for lightweight debug helpers during Sprint 1.

Current debug helpers:
- `BoardDebugPrinter`: builds a readable board-string dump for logs
- `PrototypeSessionDriver`: can run a test swap from the Inspector context menu

Suggested next debug tools:
- board coordinate overlay
- match group log with coordinate lists
- move counter and goal snapshot debug panel
